{
  "name": "devloop-v2",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "pipeline": "pnpm --filter pipeline pipeline",
    "schema:apply": "pnpm --filter pipeline schema:apply",
    "api": "pnpm --filter api start",
    "web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
