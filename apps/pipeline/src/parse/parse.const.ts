/**
 * 업무 본문과 댓글 excerpt 의 저장 상한이다.
 *
 * 6,000자는 임의 값이 아니다. 평가 세트가 필수 근거로 지목한 댓글 14건(길이 470~5,061자)을
 * 전부 담는 최소값이다. 2,000자면 10/14, 5,000자면 13/14 만 덮는다.
 * 상한을 키울수록 답변 합성 프롬프트가 커지므로 여유로 더 올리지 않는다.
 */
export const TASK_BODY_LIMIT = 6000;
export const COMMENT_EXCERPT_LIMIT = 6000;
