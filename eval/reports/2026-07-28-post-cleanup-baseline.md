# 새 기준선 — DOCUMENTS 반영·REFERENCES 정리·질의 모델 정정 후 3회 측정

- 측정일: 2026-07-28
- 대상 커밋: `4e79013` (main)
- 측정 횟수: **34문항 × 3회** (전부 실제 측정. 부분 재측정 없음)
- 소요: 회당 약 1,174초, 총 약 59분
- 판정 기준: `docs/EVAL-RUBRIC.md` 의 R 필수 축 (gold `required` 전부 인용)

## 이 측정이 기준선을 새로 만드는 이유

`2026-07-27-axis-baseline.md` 와 **직접 비교할 수 없다.** 세 가지가 동시에 바뀌었다.

| 변경 | 내용 |
| --- | --- |
| 질의 모델 | `gpt-5.6-sol` → `gpt-5.6-terra` |
| 그래프 | `REFERENCES` 765 → 328 (오탐 437건 제거) |
| 질의 프롬프트 | 스키마 힌트에 `[:MENTIONS|DOCUMENTS]` 지시 추가 |

특히 모델 변경은 의도한 것이 아니라 **버그 수정의 결과**다.
`.env` 에 `QUERY_LLM_MODEL=gpt-5.6-terra` 가 있었지만 API 가 그 파일을 읽지 않아
`codex exec` 가 `-m` 플래그 없이 실행됐고, codex 기본 모델로 질의가 돌고 있었다.
즉 이전 측정들은 문서에 확정됐다고 적힌 모델이 아닌 모델로 이루어졌을 가능성이 있다.

## 결과

판정 대상 27문항이다 (`groundTruthCypher` 7문항은 행 일치율 수동 판정 대상이라 제외).

```
전회 통과 14: A-02 A-07 A-08 A-09 A-11 A-15 H-01 H-02 H-04 H-06 H-09 H-13 H-17 H-18
전회 실패 11: A-06 A-10 A-14 H-07 H-08 H-10 H-11 H-12 H-14 H-15 H-16
변동  2: H-03 H-05
```

3회 모두 오류 0건, 근거 0건 문항 0건이었다.
이전 기준선에서 여러 문항이 "근거에 없습니다" 로 끝났던 것과 대비된다.

### 실행 간 변동이 크게 줄었다

| 측정 | 변동 문항 비율 |
| --- | --- |
| 이전 (main 3회) | 7문항 중 2문항 = **29%** |
| 이번 | 27문항 중 2문항 = **7.4%** |

변동 문항은 H-03 과 H-05 두 건이고 둘 다 `P F P` 패턴이다.
원인을 이번 측정으로 가르지는 못했다 — 모델 변경과 그래프 정리가 함께 들어갔다.

## DOCUMENTS 개선의 효과

A-07 이 목표 문항이었고 **3회 전부 통과**했다.

- 질문 — "Concept 'ingress-nginx' 를 MENTIONS 하는 Task 와 Wiki 를 관계 유형과 함께 나열해줘"
- gold 위키 `ingress-nginx 컨트롤러를 여러 개 둘 때 격리하는 법` 이 근거에 포함됐다 (손으로 대조 확인)
- 이 위키는 개념에 `DOCUMENTS` 로 붙어 있어 `[:MENTIONS]` 만 매치하는 Cypher 로는 도달할 수 없었다

같은 위키를 요구하는 H-10 은 **여전히 실패**한다.
H-10 은 "잉그레스 그거 지원 종료된다던데 대응 어떻게 하기로 했지?" 로 지칭이 모호한 문항이라,
실패 원인이 관계 유형이 아니라 앵커 해석에 있다.
관계 유형 문제는 해결됐지만 앵커 문제는 남아 있다.

## A-14 — 지표는 나빠졌지만 그래프는 정직해졌다

A-14 는 이전 기준선에서 통과였고 이번에 3회 전부 실패한다. gold 중 Task 206 이 근거에 없다.

원인을 추적한 결과다.

```
삭제된 REFERENCES:
  Task:115 → Task:206   project=tc-dnsplus   ← 다른 프로젝트 참조
  Task:459 → Task:206   project=pull         ← GitHub URL 조각
```

Task 206 의 `REFERENCES` 엣지 2개가 모두 이번에 제거한 오탐이었다.
즉 **이전 통과는 가짜 관계를 타고 도달해 얻은 것**이다.

Task 206 자체는 여전히 도달 가능하다 — `MENTIONS CloudTrail` 이 남아 있다.
연결이 끊긴 것이 아니라, 가짜 엣지가 사라져 근거 상한 30건 안에 들지 못하게 됐다.

이것을 회귀로 볼지 정정으로 볼지는 사람이 판단해야 한다.
숫자는 나빠졌지만 무환각(P)·그래프 정합(G) 축에서는 개선이다.

## REFERENCES 정리 결과

| 단계 | 총 건수 | 자기참조 | 타 프로젝트 |
| --- | --- | --- | --- |
| 이전 | 786 | 202 (26%) | 258 (33%) |
| URL·자기참조 필터 후 | 361 | 0 | 33 |
| 타 프로젝트 차단 후 | **328** | **0** | **0** |

786건 중 458건(58%)이 오탐이었다. 원인은 참조 패턴이 `단어/숫자` 를 전부 업무 참조로 본 것이다.
`github.com/.../pull/52` 가 "프로젝트 `pull` 의 52번 업무" 가 되고,
본문이 자기 번호를 언급하면 자기참조가 생겼다.

재적재 후 노드 3,088개가 전부 복원됐고 `REFERENCES` 를 뺀 나머지 관계 14종은 한 건도 달라지지 않았다.

## 판정기에 대한 단서

이번 판정은 새로 작성한 스크립트로 했다. 이전 기준선의 판정 스크립트는 남아 있지 않다.
그래서 기준선 대조로 판정기를 검증하는 대신, 1회차 결과에서 두 문항을 손으로 대조했다.

- A-07 (PASS 판정) — gold 위키가 근거에 실제로 있음을 확인
- A-14 (FAIL 판정) — Task 26·83·481 은 있고 206 만 없음을 근거 30건 전수로 확인

문항별로 이전 기준선과 비교할 때는 이 차이를 감안해야 한다.

## 남은 실패 11문항의 성격

| 유형 | 문항 | 성격 |
| --- | --- | --- |
| 원천에 근거 없음 | A-06, A-10, H-12 | 483↔491, 501↔502 엣지가 원천 데이터에 없다. 검색 개선으로 못 푼다 |
| 앵커 해석 실패 | H-10, H-11, H-16 | 지칭이 모호해 엉뚱한 앵커를 잡는다 |
| 근거 상한 밀림 | A-14 | 경로는 있으나 상한 30건 안에 못 든다 |
| 기타 | H-07, H-08, H-14, H-15 | 개별 확인 필요 |

자세한 분류는 `2026-07-28-gold-reachability.md` 에 있다.

## 다음 측정 때 줄일 수 있는 비용

질의 1건당 codex 호출은 3~5회다 (앵커 추출·Cypher 생성·조건부 재생성·조건부 근거 Cypher·답변 합성).
34문항 3회면 대략 360회다.

| 방안 | 절감 | 손실 |
| --- | --- | --- |
| `groundTruthCypher` 7문항을 반복 측정에서 제외 | 23% (실측 271초/1,174초) | 없음. 이미 자동 판정에서 빠져 있다 |
| 답변 합성 생략 모드 | 25~30% | P 축 판정 불가. 최종 1회만 켜면 해소 |
| 단계별 추론 강도 분리 | 미측정 | 품질 영향을 측정으로 확인해야 한다 |
| 응답 캐시 | 큼 | **측정에 쓰면 안 된다.** 변동성이 0으로 보여 3회 측정의 의미가 사라진다 |

앞의 두 개만 적용해도 다음 3회 측정이 절반 아래로 떨어진다.
